// src/components/Resume.js
import React from 'react';

const Resume = () => {
    return (
        <section id="resume">
            <h2>Resume</h2>
            <a href="resume.pdf" className="btn">Download Resume</a>
            {/* Optionally, you can add key resume sections here */}
        </section>
    );
};

export default Resume;
